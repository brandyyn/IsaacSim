# Machine-Learning Progress

This is the shared progress log. Update it whenever the FEA model, simulator environment, training configuration, or evaluation result changes.

## Current state

- Phase: `0 - deterministic simulation baseline`
- Baseline commit: `5131a9740b3ce82e42331e923e1a45ffa396f71c`
- Simulation: `panel_crease_leg_v8.usd`, launched with `open_knee_gui.py`
- Constraint experiment: `panel_crease_leg_constraints_v2.usd`, rebuilt with `build_compliant_panel_crease_leg_live.py` and launched with `open_compliant_panel_crease_gui.py`; not the ML baseline
- Latest constraint experiment: `panel_crease_leg_constraints_v3.usd`, rebuilt with `build_optimized_constraint_panel_crease_leg_live.py` and launched with `open_optimized_constraint_panel_crease_gui.py`; not the ML baseline
- Latest geometry-specific experiment: `panel_crease_leg_constraints_v4.usd`, rebuilt with `build_facet_crease_panel_leg_live.py` and launched with `open_facet_crease_panel_gui.py`; not the ML baseline
- Latest coupled-motion experiment: `panel_crease_leg_constraints_v8_geometry_fold.usd`, rebuilt with `build_coupled_fold_panel_leg_live.py` and launched with `open_coupled_fold_panel_gui.py`; not the ML baseline
- Latest motion pass: the geometry-fold launcher drives one physical `Physics/KneeActuator` from exact neutral with a smooth cosine command through the provisional 15° shell-safe envelope. Independent lateral/twist gimbals are not authored, and the generated asset contains no fallback crease cylinders or curve layer. The default showcase uses the same shared 50-panel/76-edge map with two deterministic rigid-facet projection passes, anchors both flat interfaces, and avoids the blocking numerical optimizer; live timing measured about 41–43 FPS versus about 3–4 FPS for solver mode. Neutral anchor error is `0.0 m`; the fast 15° endpoint remains a reduced-order display approximation and is not an FEA-calibrated strain result. This is a geometry/visual validation experiment, not the ML baseline.
- Current presentation showcase: `panel_crease_leg_constraints_v9_showcase.usd`, rebuilt with `build_panel_crease_showcase_live.py` and launched with `open_panel_crease_showcase_gui.py`; not the ML baseline. It keeps the original 50 surfaces/28 shared vertices/76 crease edges and adds three physical serial revolutes (X lateral ±22°, Z twist ±30°, Y fold −1.5°…127°). The launcher settles at exact neutral before enabling a conservative coupled envelope; the batched baked runtime measured `54.93 FPS` over a full 1,800-update cycle while commanding a bounded 60° fold peak together with ±16.5° lateral and ±22.5° twist. This is a reduced-order presentation envelope, not FEA-calibrated capacity.
- FEA case: `compression_v1` imported and replayed; frame mapping still requires FEA-owner confirmation
- ML policy: `none trained yet`
- Last validated behavior: full-scale 639.69 N compression replay completed at neutral and 45 degrees with interface translation below `1e-12 m`
- Current owner/action: provide FEA displacement/angle and reaction-torque channels, plus frame/material/boundary-condition metadata

## Roadmap

1. Package and review the FEA geometry/results handoff.
2. Map FEA coordinates and units to the Isaac Sim joint frame.
3. Fit or tabulate an FEA-derived mechanical response model.
4. Validate static and dynamic response in Isaac Sim without ML.
5. Implement the stabilization environment, observations, actions, rewards, and disturbances.
6. Run reproducible training and evaluation experiments.

## Progress log

| Date | Owner | Phase | Git commit | FEA case | Result | Next action |
|---|---|---|---|---|---|---|
| 2026-08-22 | Codex | Simulation baseline | `5131a974` | None | v8 portable physics/visual-controller checkpoint committed and pushed | Import the first reviewed Ansys FEA case |
| 2026-08-22 | Codex | FEA load replay | `6b6732d8` | `compression_v1` | 52 samples, 639.69 N peak force, neutral/45° replay stable | Add angle/displacement/torque data and confirm frame mapping |
| 2026-08-24 | Codex | Multi-DOF motion experiment | `uncommitted; base 8fcbb672` | `compression_v1` not applied | Separate three-revolute X/Z/Y variant validated with combined positive and negative targets; no ML training run | Review axis/frame mapping against the physical videos and FEA before making this an ML environment |
| 2026-08-24 | Codex | Constraint-focused compliant shell | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | Original 50-triangle/76-edge topology rebuilt with shared vertices, hard interface anchors, bounded edge constraints, and stable multi-axis validation; no ML training run | Supply FEA displacement/angle/torque data and calibrate the measured v2 edge-strain response before ML |
| 2026-08-24 | Codex | Global least-strain constraint shell | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | V3 removed v2 edge-order bias with a bounded global normalized-strain solve; combined-positive max strain improved from ~0.445 to ~0.209 with zero anchor error; no ML training run | Supply FEA displacement/angle/torque data and fit the compliant response before ML |
| 2026-08-24 | Codex | Facet-rigid crease solver experiment | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | V4 embeds all 50 source surfaces (48 triangles + 2 interface quads), solves 52 local rigid solver triangles over one shared 28-vertex map, preserves hard interface anchors, and settled without oscillation; combined-positive max facet edge distortion ~0.2196 with zero anchor error; no ML training run | Compare facet deformation against measured video/FEA displacement and calibrate the local/global weights before ML |
| 2026-08-24 | Codex | Coupled video-profile fold response | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | V5 replaces independent X/Z/Y demo sinusoids with one `foldCommandDeg`; the provisional video-derived profile maps 30°→(X=2.0°, Z=1.6°), 60°→(4.0°, 3.2°), and 90°→(6.0°, 4.8°), with zero anchor error through the sweep; no ML training run | Replace the provisional visual profile with metrology/FEA angle-displacement/torque data and rerun calibration |
| 2026-08-24 | Codex | Canonical reference-crease visual pass | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | V6 embeds the canonical one-edge-per-fold-line registry, rejects duplicate source edges, hides legacy duplicate crease layers, and validates 76/76 source-to-stage edges; coupled smoke test retained zero anchor error; no ML training run | Compare the canonical crease visibility against the physical video from the same camera angle, then replace the provisional motion profile with metrology/FEA data |
| 2026-08-24 | Codex | Coupled-fold overlap correction | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | V7 keeps the exact 76-edge topology, adds a light non-adjacent crease-midpoint separation barrier, and enforces a geometry-valid 0–60° fold range; a requested 90° command clamps to 60°; no ML training run | Calibrate the self-contact limit and fold response from measured displacement/angle/torque data before expanding the range or training |
| 2026-08-24 | Codex | Neutral-start twist sweep | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | Launcher resets the inherited 30° knee target to zero and drives the authored `TwistJoint` stops (−30°…+30°) with a dedicated smooth sweep; no ML training run | Confirm torsional stop angles/torque against the physical joint and FEA before using this motion as a training action range |
| 2026-08-24 | Codex | Neutral-start full motion envelope | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | Corrected the 1.8 mm plate/link anchor mismatch; paused neutral pose is exact; envelope visits lateral ±22°, twist ±30°, and fold 60° with zero anchor/self-clearance violation in endpoint checks, plus a 0.75-scaled combined pass; no ML training run | Validate extreme stop torques and simultaneous coupled response against physical measurements/FEA before ML |
| 2026-08-24 | Codex | Deterministic scalable showcase runtime (opt-in) | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | Added shared-vertex pose interpolation and a single 76-segment crease curve as an opt-in performance experiment; reduced the authored physics scene from 1000 Hz to 120 Hz with 8/2 iteration caps; live runtime measured ~66 FPS during the full envelope, but the interpolated surface did not preserve the natural facet fold structure, so it is not the canonical visual path; no ML training run | Keep the facet-rigid visual path canonical and profile it before introducing further optimizations |
| 2026-08-25 | Codex | Facet-rigid visual restore | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | Rebuilt `panel_crease_leg_constraints_v7_solver_restore1.usd` with the facet-rigid solver as the default, restored all 50 source surfaces and 76 individual crease cylinders, hid the simplified curve network in solver mode, and verified the supported envelope live with zero anchor/self-clearance violation; no ML training run | Compare the restored facet motion against the physical videos and calibrate the coupled profile with measured FEA displacement/torque data |
| 2026-08-25 | Codex | Async facet showcase runtime | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | Moved the numeric facet solve off the Kit render thread, fixed repeated updater creation caused by unstable stage-wrapper comparison, cached the visual path, and added an exact 76-segment crease preview plus a bounded `showcase` trajectory. Live motion measured ~34 FPS versus ~5 FPS for the synchronous solver; neutral targets remained exactly zero with zero anchor error and the validated sample had zero clearance violation; no ML training run | Compare the showcase trajectory against the physical videos and calibrate the solve rate, torque, and fold profile with measured FEA data |
| 2026-08-25 | Codex | Rigid crease-render correction | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | Replaced the shear-producing linear vertex transition with rigid-facet interpolation/projection, restored the authored crease appearance using one combined six-sided mesh for all 76 source edges, and removed the arbitrary showcase modulation in favor of sequential authored stops. Live showcase measured ~20 FPS with the crease structure visible; paused neutral measured ~64 FPS; no ML training run | Compare the visible fold sequence against the physical videos and calibrate displacement/torque response with FEA before ML |
| 2026-08-25 | Codex | Slow smooth showcase and crease-layer cleanup | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | Rebuilt `panel_crease_leg_constraints_v7_showcase_rigid_mesh3.usd`; the welded crease mesh is now enforced as the only render layer, preventing the authored fallback cylinders from reappearing at the floor origin. Showcase phase rate reduced from 0.06 Hz to 0.03 Hz and waypoint easing changed to quintic smootherstep for continuous velocity/acceleration. Live runtime measured ~34 FPS with zero anchor/self-clearance violation; no ML training run | Compare the slower direction changes against the physical videos and calibrate displacement/torque response with FEA before ML |
| 2026-08-25 | Codex | Geometry-coupled knee correction | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | Replaced the independent X/Z/Y gimbal showcase with one physical `KneeActuator`; removed the unused crease-cylinder/curve fallback from the generated stage; restored exact source geometry at neutral; launched `panel_crease_leg_constraints_v8_geometry_fold.usd` with a slow 0→15°→0° cosine fold. Live checks: 0 cylinders, no gimbal joints, zero neutral anchor/facet error, and zero anchor/self-clearance violation at the 15° endpoint; no ML training run | Supply FEA displacement/angle/torque data before extending the shell envelope toward the paper's 127° actuator stop or creating ML actions |
| 2026-08-25 | Codex | Scalable deterministic facet runtime | `uncommitted; base 8fcbb672` | `compression_v1` not calibrated | Replaced the 3–4 FPS per-frame numeric showcase solve with a deterministic rigid-facet projection over the same 50 surfaces, 28 shared vertices, and 76 crease edges; clean live reload measured about 41–43 FPS with one motion task, one visual task, zero anchor error, zero cylinders, and zero gimbals. No commit or ML run. | Keep solver mode for validation/FEA calibration; provide measured interface displacement/angle/torque before extending the 15° visual envelope or starting ML |

| 2026-08-25 | Codex | Physical multi-axis showcase v9 | `3738a39b` | `compression_v1` not calibrated | Added serial physical X/Z/Y showcase revolutes while retaining the original 50-surface/76-edge shell and batched visual runtime. Neutral settle precedes motion; the conservative coupled cycle commanded fold 0…60° together with lateral −16.5…+16.5° and twist −22.5…+22.5°, while the authored individual stops remain ±22°/±30°. A clean 1,800-update live probe measured 54.93 FPS with no controller errors. No ML run. | Compare the coupled envelope with metrology and FEA displacement/torque data before promoting it to the ML environment |

## Run index

Add one row for every completed training or evaluation run. The detailed record belongs in `ml/runs/<run-id>/`.

| Run ID | Baseline commit | FEA case | Seed | Algorithm | Evaluation result | Checkpoint |
|---|---|---|---:|---|---|---|
| None yet | — | — | — | — | — | — |
